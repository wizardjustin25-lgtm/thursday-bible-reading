import { sqliteTable, text, integer, index } from "drizzle-orm/sqlite-core";
export const questions = sqliteTable("questions", { id:text("id").primaryKey(),passage:text("passage").notNull(),content:text("content").notNull(),name:text("name").notNull(),summary:text("summary"),summaryStatus:text("summary_status").notNull().default("unavailable"),createdAt:integer("created_at").notNull() }, table=>[index("idx_questions_created_at").on(table.createdAt)]);
export const requestLimits=sqliteTable("request_limits",{key:text("key").primaryKey(),hits:integer("hits").notNull(),expiresAt:integer("expires_at").notNull()},table=>[index("idx_request_limits_expires_at").on(table.expiresAt)]);
